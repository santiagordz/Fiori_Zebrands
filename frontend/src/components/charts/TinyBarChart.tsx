import {
  BarChart,
  Bar,
  Cell,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
} from "recharts";

import React, { FC } from "react";

interface TinyBarChartProps {}

const TinyBarChart: FC<TinyBarChartProps> = ({}) => {
  return <div>TinyBarChart</div>;
};

export default TinyBarChart;
